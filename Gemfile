source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins
gem "jekyll-remote-theme"  # Ensure this is included

# Optional: Only include this if you're on Windows to avoid issues with file watching
gem 'wdm', '>= 0.1.0' if Gem.win_platform?
